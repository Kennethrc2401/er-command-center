// const formatName = (name: string) => {
//   if (!isDemoMode) return name;
//   const parts = name.split(' ');
//   return parts.length > 1 ? `${parts[0][0]}. ${parts[1]}` : name;
// };

// Use it like this:
// <div className="font-black">
//   {formatName(e.patientName)}
// </div>